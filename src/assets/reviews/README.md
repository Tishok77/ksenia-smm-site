# Review screenshots

Add screenshots of real reviews here (e.g. `review-01.jpg`). Wire them into
`src/data/reviews.js` by importing the file and filling in `screenshot`,
`quote` and `name` for the matching entry — do not invent review text,
leave entries as placeholders until real feedback is available.
